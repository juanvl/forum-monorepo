{
  "name": "forum-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n docker,studio,api -c yellow,blue,green \"sh -c 'cd apps/api && docker compose up'\" \"sh -c 'wait-on tcp:5432 && pnpm --filter @forum/api studio'\" \"sh -c 'wait-on tcp:5432 && pnpm --filter @forum/api start:dev'\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.1"
  }
}